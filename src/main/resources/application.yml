spring:
  application:
    name: challenge
  datasource:
    url: jdbc:postgresql://localhost:5432/posfiap
    driver-class-name: org.postgresql.Driver
    username: posfiap
    password: root

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always